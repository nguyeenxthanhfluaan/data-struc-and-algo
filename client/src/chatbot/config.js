import { createChatBotMessage } from 'react-chatbot-kit'

const config = {
	initialMessages: [
		createChatBotMessage(
			`Xin chào! Tên tôi là Daisy. Tôi có thể giúp bạn trả lời một số định nghĩa!`
		),
	],
}

export default config
